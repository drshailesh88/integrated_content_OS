{
  "mcpServers": {
    "pubmed-mcp-server": {
      "command": "node",
      "args": ["pubmed-mcp-server/dist/index.js"],
      "env": {
        "NCBI_API_KEY": "${NCBI_API_KEY}",
        "MCP_LOG_LEVEL": "info"
      }
    }
  }
}
